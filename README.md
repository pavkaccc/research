# research

Monorepo for research projects.

## Projects

| Project | Path | Description |
|---------|------|-------------|
| **divkit-rs** | [`divkit-rs/`](divkit-rs/) | Rust port of [DivKit json-builder](https://github.com/divkit/divkit/tree/main/json-builder/python). Provides entity types with `build()`/`dict()` and `schema()` methods for constructing DivKit JSON layouts programmatically. |
